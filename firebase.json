{
  "firestore": {
    "rules": "firestore.rules",
    "indexes": "firestore.indexes.json"
  },
  "storage": {
    "rules": "storage.rules"
  },
  "apphosting": [
    {
      "backendId": "fixed-income-tracker",
      "rootDir": "/",
      "ignore": ["node_modules", ".git", ".next", "firebase-debug.log", "*.local"]
    }
  ]
}
